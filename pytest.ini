[pytest]
DJANGO_SETTINGS_MODULE = core.settings
python_files = tests.py test_*.py *_tests.py
addopts = -ra -q
testpaths = tests
markers =
    slow: marks tests as slow
    unit: fast, no DB
    integration: needs DB/external boundaries
    auth: auth-related tests
